# MIearn 长期工程记忆

这份文档记录 MIearn 开发中已经验证过的工程约束、常见陷阱和处理方法。它面向后续维护者，不保存密码、Secret、keystore 内容、个人目录或设备隐私。

## 1. 不可破坏的产品与架构约束

- Android 原生工程使用 Kotlin、Jetpack Compose、Room、MVVM、Coroutines/Flow 和 Material 3。
- 核心学习、搜索、复习和内置音频必须可以离线运行，Manifest 不应声明 `INTERNET`。
- Room 中的学习进度、收藏和错题绑定 `wordId`。仅调整展示方式时不要拆分 ID、升级数据库或覆盖用户进度。
- 新词学习保持“两遍”：第一遍浏览，不要求判断；第二遍英选中。答题后按既有节奏自动进入下一词。
- SM-2、学习队列、正误判定和会话恢复属于高风险区域。视觉改动不得顺带重写这些逻辑。
- 首页的主要动作始终是底部大号“开始学习”按钮，其他信息不能争夺首要注意力。

## 2. 多表达、句子与主表达

- `EnglishVariantParser` 是唯一切分入口，UI、音频和详情不能各写一套分隔规则。
- 术语和句子必须按 `kind` 分别处理。句子不能简单按空格、斜杠或普通标点逐词拆开。
- `/` 和 `\` 可能是表达分隔符，也可能属于单位或缩写，例如 `300mm/s`、`I/O`。修改规则时必须保留对应测试。
- 主表达选择顺序：
  1. 对解析结果做完整、忽略大小写的 `primaryEnglish` 匹配。
  2. 匹配失败时使用首个有效完整片段。
  3. 不允许为了匹配而把完整短语截成单个词。
- 学习页采用“一主多辅”：第一遍只突出主表达，其他术语折叠为“另有 N 种表达”；句子折叠为“相近说法”。
- 第二遍、到期复习和错题强化只考主表达。搜索与详情仍展示完整词条。
- 新建 `LearningSession` 时按输入顺序去重，复习 ID 优先于新词 ID；不要让同一 `wordId` 在一次任务中重复出现或跨阶段重复占位。
- 五种测试模式也必须通过 `QuizContentPolicy.primaryEnglish()` 取主表达；不能在英选中题干重新使用完整复合英文，否则会把同义表达一起暴露给用户。
- 五种测试模式都保留“重播主表达发音”入口，必须调用主表达索引而不是完整播放，避免多表达题目读音与题干不一致。
- 多表达仍共享一个 `wordId` 的 SM-2、收藏和错题状态，除非未来产品明确要求变体级掌握。
- 错题强化答对时应降低 `wrongCount`，答错时增加；强化写入必须依据会话的阶段/索引幂等，避免进程恢复或重复点击造成错误权重翻倍。

## 3. 音频链路

- 完整音频路径与分段音频路径是稳定契约。分段索引必须与 `EnglishVariantParser` 返回的原始索引一致。
- 学习卡自动播放主表达时调用 `playVariant(word, primaryIndex)`，不能调用完整 `play(word)`。
- “播放全部”是用户主动操作，多个片段间保留约 500ms 静音。
- 点击任意片段前应停止当前完整播放，避免不同发音重叠。
- 内置音频失败后才回退 TTS；单片段失败只回退对应片段，不应重新朗读整条内容。
- 正误答题音效使用 `SoundPool` 的 `USAGE_ASSISTANCE_SONIFICATION` 与 `CONTENT_TYPE_SONIFICATION`；不要改成媒体音量通道，否则部分设备会静音或被媒体焦点策略抑制。
- 退出学习、详情或应用生命周期结束时必须停止并释放播放器。历史上出现过退出页面仍继续发音的问题。
- TTS 初始化失败本身不等于整条播放失败。只有内置资源和 TTS 都失败时才显示帮助提示。
- 正确、错误提示音使用独立 SoundPool。每次有效答案只触发一次，并在播放前停止词条发音。

## 4. Room、自定义词库与提醒

- 内容升级应以事务导入，并保留已有 `ProgressEntity`；不要通过重新建库解决内容差异。
- 自定义词库删除时先删除来源关系，只有不再属于任何来源的自定义词才可删除。
- 自定义来源名称必须对用户可区分：提交时对同名来源生成 `名称 (2)`、`名称 (3)` 等后缀，并把最终名称同时写回 `sources`、完成后的 `import_jobs` 和新词条标签，避免首页下拉出现两个无法辨认的来源。
- 重命名自定义来源也必须复用同一套唯一名称规则，并同步已完成导入记录；否则来源列表、历史导入记录和词条标签会出现不一致。
- 重命名对话框对空白名称必须给出内联错误并禁用保存；不要静默关闭让用户误以为已保存。
- 自定义来源重命名必须与其自定义词条的 `categoryLabel` 在同一事务中同步；独占词条使用来源名，跨来源复用的自定义词条使用“自定义词条”，内置词条被复用到该来源时不要改写其全局标签。
- 导入文件必须经过清洗、列映射、冲突确认和异步提交；UI 退出不能破坏 WorkManager 中的任务。
- 学习提醒必须由 WorkManager/系统调度触发，不能依赖应用启动后补发。
- 修改提醒时间后立即重排任务；Android 13+ 只在用户开启提醒时申请通知权限。
- 时间选择、通知权限和系统省电策略都需要真机回归，模拟器只能验证基本链路。

### 导入文件兼容性（V2.34 以后）

- SAF 返回的文件名和 MIME 不能作为唯一判断依据；先把任务写入 Room，再把源文件复制到 `noBackupFilesDir/imports`，最后用内容签名判断格式。不要把可恢复的 WorkManager 导入副本放在 `cacheDir`，系统可能在后台清理它。
- 支持 `.xlsx`、`.csv`、`.tsv`、`.txt`，文本解码顺序为 UTF-8/BOM、UTF-16LE/BE（含常见无 BOM 文件）、GB18030；旧版二进制 `.xls`、普通 ZIP 和未知二进制必须给出可操作的转换提示。
- 格式探测也必须使用解码后的样本：当 SAF 给出 `application/octet-stream` 和无扩展名文件时，无 BOM UTF-16 仍要能识别制表符/逗号结构，不能只把原始 NUL 字节样本当作未知二进制。
- XLSX 的富文本单元格可能由多个 `<r><t>` 片段组成；读取 `inlineStr` 时必须拼接所有 `<t>`，不能在每个片段开始时清空缓冲区，否则带粗体/颜色的词条会被截断。
- 多例句沿用 `exampleEn/exampleZh` 字段，以相同顺序的换行分隔；术语流水线生成两条例句，短语继续保留人工原文。UI 必须按换行成对展示，并对旧的单行数据回退为一个例句，不能新增 Room 字段或破坏音频文本。
- 例句播放使用 `SpeechRequestFactory.text()` 走现有 TTS 状态机；每次只提交一条例句，点击前会打断词条或其他例句发音，退出页面仍由统一音频生命周期负责停止。
- 纯 TXT 默认“一行一个词条”；只有制表符列数稳定时才按 TSV 解析。不要把分号自动拆开，以免破坏多英文表达。
- 读取、复制或查询 SAF 元数据失败时也要落成可观察的 `FAILED` 任务，不能只抛异常让界面停在原地。失败界面必须显示错误码、原因、解决建议，并可重新选择。
- 格式损坏、编码错误、行数/文件大小超限和无有效词条属于永久失败，清理应用私有副本；权限/I/O 等临时错误才保留副本并允许重试，避免“重试”按钮对不可修复文件反复循环。
- 复制阶段由 SAF 流驱动，用户取消或进程被系统回收后不能假设可以续传：复制循环要响应取消，发布副本必须用 `status = COPYING` 条件更新；启动时将遗留 COPYING 任务转成可重新选择文件的失败状态，并清理 `.source/.partial`。
- 取消与异常可能在同一时间到达：复制、准备和提交阶段的失败写入必须使用带状态条件的 Room `UPDATE`，Worker 入口也要短路已取消任务；不能先读出旧实体再无条件 `upsert`，否则会把 `CANCELLED` 回写成 `FAILED`，造成 UI 状态回跳。
- 测试答错会增加 `wrongCount`，之后答对应通过同一降权入口递减，且不低于 0；错题强化和测试重测都要保持这个口径。
- 自定义小词库若不足两个不同答案，选择题必须进入可操作的空状态，不能生成只有一个选项的伪测试；拼写和例句填空仍可使用单条词库。
- Room 新增导入错误字段必须提供显式迁移；所有从旧版本数据库升级的测试都要覆盖完整迁移链。

### 内置内容数量口径（V2.31+）

- 原始 PDF/Excel 抽取阶段仍有 2,704 条记录：机械 1,227、电气 970、客户评审 251、会议 58、商务 198。
- V2.31 清洗会排除 6 个已审计的章节标题伪条目（客户评审 5 个、会议标题 1 个），所以应用最终可学习内容固定为 2,698 条：机械 1,227、电气 970、客户评审 246、会议 57、商务 198。
- `tools/data/content_v231_repairs.json` 是排除清单的唯一来源；不要为了恢复旧的 2,704 数字而凭空补词或改动稳定 ID。内容流水线测试必须同时验证原始计数、排除数量和最终计数。

## 5. Compose UI 与动画

- 小屏、大字号和系统返回手势是默认验收项，不是发布前才补的适配。
- 主要卡片约 24–28dp 圆角，列表卡片约 18dp，控件约 14–20dp；新增组件应复用主题色和现有空间层级。
- 动画必须短、克制，并避免驱动整个页面重组。持续动画仅用于用户明确要求的活跃火苗。
- 火苗由 Compose Canvas 绘制，不引入 Lottie、GIF 或大型图片资源。
- 火苗分级保持：1–2 天小火、3–6 天中火、7 天以上大火。
- 页面不在组合树、应用进入后台或系统关闭动画时，动画应暂停或退化为静态效果；`StreakFlame` 通过生命周期状态和三个系统动画缩放值统一判断，调用方不要绕过该策略自行启动无限动画。
- 新视觉先用仓库外的临时浏览器稿验证，再落地 Compose；预览文件和服务不得进入提交。
- 学习数据页的正确率必须区分“无样本”和“0%”：无答题样本显示“—”，至少有一次答题才显示百分比。
- 首页没有新词或到期复习时，主按钮显示“今日已完成”并禁用；学习入口不能把空会话伪装成一次完成任务，空完成态要说明明天再来或更换词库。
- 首页“今日新学”必须按当前词库扣除当天已首次学会的词条，不能只用全库未学数量与每日目标，否则完成部分任务后仍会显示原始数量；复习数量继续直接读取当天到期项。
- 首页统计若存在当天、当前词库的未完成 `study_session`，必须优先显示会话剩余量；错题强化也要保留一个可恢复的复习入口，不能因数据库到期数暂时为零而把中途会话伪装成“今日已完成”。
- 日期相关首页数据不能只在 ViewModel 创建时读取一次；回到前台时刷新 epoch day，使跨午夜后的连续天数、今日任务和会话匹配条件立即更新。
- 拼写/填空题输入是题目级瞬态状态，不要只用题号或 `rememberSaveable` 跨测试轮复用；按 `mode + wordId + index` 重建输入状态，避免新一轮测试残留旧答案。
- 拼写和例句填空进入新题时可自动请求输入焦点并调起键盘；选择题与听音题不要自动弹键盘，避免遮挡选项。焦点请求必须跟随题目键变化，答题后禁用输入框。

## 6. 发布、安全与体积

- Release 签名密钥始终保存在仓库外，本地通过 `key.properties` 或环境变量注入，CI 通过 GitHub Secrets 注入。
- 禁止提交 `*.jks`、`*.keystore`、`key.properties`、`.env`、构建日志和虚拟环境。
- 创建标签前先确认 `versionName`、递增的 `versionCode`、标签名和 Release 标题一致。
- 发布顺序：完整测试与 Lint → 版本修改 → 推送主分支 → 推送标签 → CI 签名构建 → 校验 APK → 创建 GitHub Release。
- Release 必须验证签名、SHA-256、包名、版本号和权限表面。
- Debug/Release 体积门禁由 Gradle 任务负责，不要通过删除必要离线内容来机械压缩 APK。

## 7. 最小验证清单

每次功能修改至少完成：

1. 为行为变化先增加能够失败的测试。
2. 运行目标测试并确认失败原因是功能尚未实现。
3. 做最小实现，让目标测试通过。
4. 运行相关模块测试和回归测试。
5. 执行 `assembleDebug`、`test`、`lint` 和 `verifyDebugApkSize`。
6. 检查 Git diff，确认没有版本号、签名配置、生成物或无关文件变化。
7. 涉及 UI 时检查浅色、深色、小屏和大字号。
8. 涉及音频时断网验证完整音频、单片段、打断播放和退出停止。

## 8. 问题记录模板

后续遇到值得复用的问题，按以下格式追加或合并，避免只记录一次性的日志：

```text
日期 / 版本：
现象：
触发条件：
根因：
解决方法：
为什么选择该方法：
防回归测试：
仍需关注：
```

记录应优先说明“为什么会发生”和“怎样防止再次发生”，而不是粘贴长日志。
